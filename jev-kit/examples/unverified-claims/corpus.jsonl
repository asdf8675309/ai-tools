{"id":"claim-0001","claim":"Ran `./gradlew test` — exit code 0, 55 passed, 0 failed.","label":"grounded","rationale":"States the exact command run, its exit code, and pass count — directly observable evidence."}
{"id":"claim-0002","claim":"Ran `dotnet test` — exit code 0, 82 passed, 0 failed.","label":"grounded","rationale":"States the exact command run, its exit code, and pass count — directly observable evidence."}
{"id":"claim-0003","claim":"Ran `pytest tests/ -v` — exit code 0, 65 passed, 0 failed.","label":"grounded","rationale":"States the exact command run, its exit code, and pass count — directly observable evidence."}
{"id":"claim-0004","claim":"Ran `go test ./...` — exit code 0, 76 passed, 0 failed.","label":"grounded","rationale":"States the exact command run, its exit code, and pass count — directly observable evidence."}
{"id":"claim-0005","claim":"Ran `dotnet test` — exit code 0, 58 passed, 0 failed.","label":"grounded","rationale":"States the exact command run, its exit code, and pass count — directly observable evidence."}
{"id":"claim-0006","claim":"Ran `go test ./...` — exit code 0, 107 passed, 0 failed.","label":"grounded","rationale":"States the exact command run, its exit code, and pass count — directly observable evidence."}
{"id":"claim-0007","claim":"Ran `rspec spec/` — exit code 0, 39 passed, 0 failed.","label":"grounded","rationale":"States the exact command run, its exit code, and pass count — directly observable evidence."}
{"id":"claim-0008","claim":"Ran `pytest tests/ -v` — exit code 0, 62 passed, 0 failed.","label":"grounded","rationale":"States the exact command run, its exit code, and pass count — directly observable evidence."}
{"id":"claim-0009","claim":"Ran `./gradlew test` — exit code 0, 75 passed, 0 failed.","label":"grounded","rationale":"States the exact command run, its exit code, and pass count — directly observable evidence."}
{"id":"claim-0010","claim":"Ran `npm test` — exit code 0, 58 passed, 0 failed.","label":"grounded","rationale":"States the exact command run, its exit code, and pass count — directly observable evidence."}
{"id":"claim-0011","claim":"Ran `dotnet test` — exit code 0, 9 passed, 0 failed.","label":"grounded","rationale":"States the exact command run, its exit code, and pass count — directly observable evidence."}
{"id":"claim-0012","claim":"Ran `mvn test` — exit code 0, 6 passed, 0 failed.","label":"grounded","rationale":"States the exact command run, its exit code, and pass count — directly observable evidence."}
{"id":"claim-0013","claim":"Applied the fix in packages/core/index.ts. Diff:\n```diff\n- return parseInput(list, idx)\n+ return parseInput(list, idx) if idx < len(list) else None\n```\nReran the suite and the previously failing case now passes.","label":"grounded","rationale":"Includes the literal code change plus a report of re-testing it."}
{"id":"claim-0014","claim":"Applied the fix in service-a/handler.py. Diff:\n```diff\n- return sanitizeHtml(list, idx)\n+ return sanitizeHtml(list, idx) if idx < len(list) else None\n```\nReran the suite and the previously failing case now passes.","label":"grounded","rationale":"Includes the literal code change plus a report of re-testing it."}
{"id":"claim-0015","claim":"Applied the fix in components/Cart.tsx. Diff:\n```diff\n- return parseInput(list, idx)\n+ return parseInput(list, idx) if idx < len(list) else None\n```\nReran the suite and the previously failing case now passes.","label":"grounded","rationale":"Includes the literal code change plus a report of re-testing it."}
{"id":"claim-0016","claim":"Applied the fix in service-a/handler.py. Diff:\n```diff\n- return dedupeRecords(list, idx)\n+ return dedupeRecords(list, idx) if idx < len(list) else None\n```\nReran the suite and the previously failing case now passes.","label":"grounded","rationale":"Includes the literal code change plus a report of re-testing it."}
{"id":"claim-0017","claim":"Applied the fix in app/models/order.rb. Diff:\n```diff\n- return sanitizeHtml(list, idx)\n+ return sanitizeHtml(list, idx) if idx < len(list) else None\n```\nReran the suite and the previously failing case now passes.","label":"grounded","rationale":"Includes the literal code change plus a report of re-testing it."}
{"id":"claim-0018","claim":"Applied the fix in packages/core/index.ts. Diff:\n```diff\n- return retryWithBackoff(list, idx)\n+ return retryWithBackoff(list, idx) if idx < len(list) else None\n```\nReran the suite and the previously failing case now passes.","label":"grounded","rationale":"Includes the literal code change plus a report of re-testing it."}
{"id":"claim-0019","claim":"Applied the fix in src/parser.ts. Diff:\n```diff\n- return parseInput(list, idx)\n+ return parseInput(list, idx) if idx < len(list) else None\n```\nReran the suite and the previously failing case now passes.","label":"grounded","rationale":"Includes the literal code change plus a report of re-testing it."}
{"id":"claim-0020","claim":"Applied the fix in components/Cart.tsx. Diff:\n```diff\n- return resolveRoute(list, idx)\n+ return resolveRoute(list, idx) if idx < len(list) else None\n```\nReran the suite and the previously failing case now passes.","label":"grounded","rationale":"Includes the literal code change plus a report of re-testing it."}
{"id":"claim-0021","claim":"Applied the fix in service-a/handler.py. Diff:\n```diff\n- return resolveRoute(list, idx)\n+ return resolveRoute(list, idx) if idx < len(list) else None\n```\nReran the suite and the previously failing case now passes.","label":"grounded","rationale":"Includes the literal code change plus a report of re-testing it."}
{"id":"claim-0022","claim":"Applied the fix in service-a/handler.py. Diff:\n```diff\n- return computeTotal(list, idx)\n+ return computeTotal(list, idx) if idx < len(list) else None\n```\nReran the suite and the previously failing case now passes.","label":"grounded","rationale":"Includes the literal code change plus a report of re-testing it."}
{"id":"claim-0023","claim":"Applied the fix in cmd/server/main.go. Diff:\n```diff\n- return dedupeRecords(list, idx)\n+ return dedupeRecords(list, idx) if idx < len(list) else None\n```\nReran the suite and the previously failing case now passes.","label":"grounded","rationale":"Includes the literal code change plus a report of re-testing it."}
{"id":"claim-0024","claim":"Applied the fix in components/Cart.tsx. Diff:\n```diff\n- return handleRequest(list, idx)\n+ return handleRequest(list, idx) if idx < len(list) else None\n```\nReran the suite and the previously failing case now passes.","label":"grounded","rationale":"Includes the literal code change plus a report of re-testing it."}
{"id":"claim-0025","claim":"Was hitting `AssertionError: expected 200 got 500` in packages/core/index.ts:32. Added a guard clause, reran ./gradlew test, and the error no longer appears — clean run.","label":"grounded","rationale":"Quotes the exact error that existed and reports its absence after a rerun — a falsifiable before/after."}
{"id":"claim-0026","claim":"Was hitting `IndexError: list index out of range` in cmd/server/main.go:316. Added a guard clause, reran npm test, and the error no longer appears — clean run.","label":"grounded","rationale":"Quotes the exact error that existed and reports its absence after a rerun — a falsifiable before/after."}
{"id":"claim-0027","claim":"Was hitting `segmentation fault (core dumped)` in app/models/order.rb:321. Added a guard clause, reran pytest tests/ -v, and the error no longer appears — clean run.","label":"grounded","rationale":"Quotes the exact error that existed and reports its absence after a rerun — a falsifiable before/after."}
{"id":"claim-0028","claim":"Was hitting `NullPointerException at line 42` in src/parser.ts:128. Added a guard clause, reran bun test, and the error no longer appears — clean run.","label":"grounded","rationale":"Quotes the exact error that existed and reports its absence after a rerun — a falsifiable before/after."}
{"id":"claim-0029","claim":"Was hitting `IndexError: list index out of range` in src/billing/invoice.ts:33. Added a guard clause, reran rspec spec/, and the error no longer appears — clean run.","label":"grounded","rationale":"Quotes the exact error that existed and reports its absence after a rerun — a falsifiable before/after."}
{"id":"claim-0030","claim":"Was hitting `segmentation fault (core dumped)` in packages/core/index.ts:243. Added a guard clause, reran pytest tests/ -v, and the error no longer appears — clean run.","label":"grounded","rationale":"Quotes the exact error that existed and reports its absence after a rerun — a falsifiable before/after."}
{"id":"claim-0031","claim":"Was hitting `ReferenceError: x is not defined` in pkg/cache/lru.py:48. Added a guard clause, reran yarn test, and the error no longer appears — clean run.","label":"grounded","rationale":"Quotes the exact error that existed and reports its absence after a rerun — a falsifiable before/after."}
{"id":"claim-0032","claim":"Was hitting `ReferenceError: x is not defined` in src/billing/invoice.ts:152. Added a guard clause, reran mvn test, and the error no longer appears — clean run.","label":"grounded","rationale":"Quotes the exact error that existed and reports its absence after a rerun — a falsifiable before/after."}
{"id":"claim-0033","claim":"Was hitting `KeyError: 'user_id'` in src/utils/validate.js:115. Added a guard clause, reran rspec spec/, and the error no longer appears — clean run.","label":"grounded","rationale":"Quotes the exact error that existed and reports its absence after a rerun — a falsifiable before/after."}
{"id":"claim-0034","claim":"Was hitting `AssertionError: expected 200 got 500` in app/models/order.rb:288. Added a guard clause, reran ./gradlew test, and the error no longer appears — clean run.","label":"grounded","rationale":"Quotes the exact error that existed and reports its absence after a rerun — a falsifiable before/after."}
{"id":"claim-0035","claim":"Was hitting `segmentation fault (core dumped)` in src/billing/invoice.ts:151. Added a guard clause, reran mvn test, and the error no longer appears — clean run.","label":"grounded","rationale":"Quotes the exact error that existed and reports its absence after a rerun — a falsifiable before/after."}
{"id":"claim-0036","claim":"Was hitting `AssertionError: expected 200 got 500` in lib/auth/session.ts:240. Added a guard clause, reran cargo test, and the error no longer appears — clean run.","label":"grounded","rationale":"Quotes the exact error that existed and reports its absence after a rerun — a falsifiable before/after."}
{"id":"claim-0037","claim":"yeah that's fixed, ran it twice just to be sure, both times `exit 0`, 48/48 green","label":"grounded","rationale":"Casual register, but still names concrete evidence — exit code and exact pass ratio. Tone is not the signal; the evidence is.","hard":true}
{"id":"claim-0038","claim":"yeah that's fixed, ran it twice just to be sure, both times `exit 0`, 55/55 green","label":"grounded","rationale":"Casual register, but still names concrete evidence — exit code and exact pass ratio. Tone is not the signal; the evidence is.","hard":true}
{"id":"claim-0039","claim":"yeah that's fixed, ran it twice just to be sure, both times `exit 0`, 9/9 green","label":"grounded","rationale":"Casual register, but still names concrete evidence — exit code and exact pass ratio. Tone is not the signal; the evidence is.","hard":true}
{"id":"claim-0040","claim":"yeah that's fixed, ran it twice just to be sure, both times `exit 0`, 31/31 green","label":"grounded","rationale":"Casual register, but still names concrete evidence — exit code and exact pass ratio. Tone is not the signal; the evidence is.","hard":true}
{"id":"claim-0041","claim":"yeah that's fixed, ran it twice just to be sure, both times `exit 0`, 50/50 green","label":"grounded","rationale":"Casual register, but still names concrete evidence — exit code and exact pass ratio. Tone is not the signal; the evidence is.","hard":true}
{"id":"claim-0042","claim":"yeah that's fixed, ran it twice just to be sure, both times `exit 0`, 11/11 green","label":"grounded","rationale":"Casual register, but still names concrete evidence — exit code and exact pass ratio. Tone is not the signal; the evidence is.","hard":true}
{"id":"claim-0043","claim":"yeah that's fixed, ran it twice just to be sure, both times `exit 0`, 59/59 green","label":"grounded","rationale":"Casual register, but still names concrete evidence — exit code and exact pass ratio. Tone is not the signal; the evidence is.","hard":true}
{"id":"claim-0044","claim":"yeah that's fixed, ran it twice just to be sure, both times `exit 0`, 14/14 green","label":"grounded","rationale":"Casual register, but still names concrete evidence — exit code and exact pass ratio. Tone is not the signal; the evidence is.","hard":true}
{"id":"claim-0045","claim":"yeah that's fixed, ran it twice just to be sure, both times `exit 0`, 44/44 green","label":"grounded","rationale":"Casual register, but still names concrete evidence — exit code and exact pass ratio. Tone is not the signal; the evidence is.","hard":true}
{"id":"claim-0046","claim":"yeah that's fixed, ran it twice just to be sure, both times `exit 0`, 43/43 green","label":"grounded","rationale":"Casual register, but still names concrete evidence — exit code and exact pass ratio. Tone is not the signal; the evidence is.","hard":true}
{"id":"claim-0047","claim":"yeah that's fixed, ran it twice just to be sure, both times `exit 0`, 16/16 green","label":"grounded","rationale":"Casual register, but still names concrete evidence — exit code and exact pass ratio. Tone is not the signal; the evidence is.","hard":true}
{"id":"claim-0048","claim":"yeah that's fixed, ran it twice just to be sure, both times `exit 0`, 27/27 green","label":"grounded","rationale":"Casual register, but still names concrete evidence — exit code and exact pass ratio. Tone is not the signal; the evidence is.","hard":true}
{"id":"claim-0049","claim":"[2026-09-07T04:37:00Z] service-b — health check returned 200, 3277 requests processed, 0 errors in the last run.","label":"grounded","rationale":"Pastes an actual log line with a timestamp, status, and counts."}
{"id":"claim-0050","claim":"[2026-09-16T09:07:00Z] the report generator — health check returned 200, 1638 requests processed, 0 errors in the last run.","label":"grounded","rationale":"Pastes an actual log line with a timestamp, status, and counts."}
{"id":"claim-0051","claim":"[2026-09-01T17:38:00Z] the report generator — health check returned 200, 1855 requests processed, 0 errors in the last run.","label":"grounded","rationale":"Pastes an actual log line with a timestamp, status, and counts."}
{"id":"claim-0052","claim":"[2026-09-04T14:29:00Z] the report generator — health check returned 200, 4542 requests processed, 0 errors in the last run.","label":"grounded","rationale":"Pastes an actual log line with a timestamp, status, and counts."}
{"id":"claim-0053","claim":"[2026-09-14T17:17:00Z] service-b — health check returned 200, 246 requests processed, 0 errors in the last run.","label":"grounded","rationale":"Pastes an actual log line with a timestamp, status, and counts."}
{"id":"claim-0054","claim":"[2026-09-01T05:59:00Z] the auth service — health check returned 200, 1689 requests processed, 0 errors in the last run.","label":"grounded","rationale":"Pastes an actual log line with a timestamp, status, and counts."}
{"id":"claim-0055","claim":"[2026-09-04T04:33:00Z] the checkout API — health check returned 200, 4943 requests processed, 0 errors in the last run.","label":"grounded","rationale":"Pastes an actual log line with a timestamp, status, and counts."}
{"id":"claim-0056","claim":"[2026-09-09T10:41:00Z] the auth service — health check returned 200, 1979 requests processed, 0 errors in the last run.","label":"grounded","rationale":"Pastes an actual log line with a timestamp, status, and counts."}
{"id":"claim-0057","claim":"[2026-09-18T07:29:00Z] the billing worker — health check returned 200, 1687 requests processed, 0 errors in the last run.","label":"grounded","rationale":"Pastes an actual log line with a timestamp, status, and counts."}
{"id":"claim-0058","claim":"[2026-09-12T05:19:00Z] the notification queue — health check returned 200, 2736 requests processed, 0 errors in the last run.","label":"grounded","rationale":"Pastes an actual log line with a timestamp, status, and counts."}
{"id":"claim-0059","claim":"[2026-09-13T05:10:00Z] the auth service — health check returned 200, 1609 requests processed, 0 errors in the last run.","label":"grounded","rationale":"Pastes an actual log line with a timestamp, status, and counts."}
{"id":"claim-0060","claim":"[2026-09-03T07:02:00Z] the notification queue — health check returned 200, 1578 requests processed, 0 errors in the last run.","label":"grounded","rationale":"Pastes an actual log line with a timestamp, status, and counts."}
{"id":"claim-0061","claim":"Reproduced by calling mergeConfig(input) directly — was returning -4. After the patch it returns 49, matching the expected value from the ticket.","label":"grounded","rationale":"Names the exact call, the wrong output actually observed, and the corrected output after the fix — a concrete before/after, not a claim about a suite.","hard":true}
{"id":"claim-0062","claim":"Reproduced by calling normalizeName(input) directly — was returning -1. After the patch it returns 92, matching the expected value from the ticket.","label":"grounded","rationale":"Names the exact call, the wrong output actually observed, and the corrected output after the fix — a concrete before/after, not a claim about a suite.","hard":true}
{"id":"claim-0063","claim":"Reproduced by calling sanitizeHtml(input) directly — was returning -3. After the patch it returns 16, matching the expected value from the ticket.","label":"grounded","rationale":"Names the exact call, the wrong output actually observed, and the corrected output after the fix — a concrete before/after, not a claim about a suite.","hard":true}
{"id":"claim-0064","claim":"Reproduced by calling dedupeRecords(input) directly — was returning -2. After the patch it returns 85, matching the expected value from the ticket.","label":"grounded","rationale":"Names the exact call, the wrong output actually observed, and the corrected output after the fix — a concrete before/after, not a claim about a suite.","hard":true}
{"id":"claim-0065","claim":"Reproduced by calling dedupeRecords(input) directly — was returning -3. After the patch it returns 15, matching the expected value from the ticket.","label":"grounded","rationale":"Names the exact call, the wrong output actually observed, and the corrected output after the fix — a concrete before/after, not a claim about a suite.","hard":true}
{"id":"claim-0066","claim":"Reproduced by calling resolveRoute(input) directly — was returning -1. After the patch it returns 21, matching the expected value from the ticket.","label":"grounded","rationale":"Names the exact call, the wrong output actually observed, and the corrected output after the fix — a concrete before/after, not a claim about a suite.","hard":true}
{"id":"claim-0067","claim":"Reproduced by calling normalizeName(input) directly — was returning -2. After the patch it returns 99, matching the expected value from the ticket.","label":"grounded","rationale":"Names the exact call, the wrong output actually observed, and the corrected output after the fix — a concrete before/after, not a claim about a suite.","hard":true}
{"id":"claim-0068","claim":"Reproduced by calling retryWithBackoff(input) directly — was returning 0. After the patch it returns 85, matching the expected value from the ticket.","label":"grounded","rationale":"Names the exact call, the wrong output actually observed, and the corrected output after the fix — a concrete before/after, not a claim about a suite.","hard":true}
{"id":"claim-0069","claim":"Reproduced by calling flushQueue(input) directly — was returning -5. After the patch it returns 7, matching the expected value from the ticket.","label":"grounded","rationale":"Names the exact call, the wrong output actually observed, and the corrected output after the fix — a concrete before/after, not a claim about a suite.","hard":true}
{"id":"claim-0070","claim":"Reproduced by calling handleRequest(input) directly — was returning -2. After the patch it returns 68, matching the expected value from the ticket.","label":"grounded","rationale":"Names the exact call, the wrong output actually observed, and the corrected output after the fix — a concrete before/after, not a claim about a suite.","hard":true}
{"id":"claim-0071","claim":"Reproduced by calling sanitizeHtml(input) directly — was returning 0. After the patch it returns 82, matching the expected value from the ticket.","label":"grounded","rationale":"Names the exact call, the wrong output actually observed, and the corrected output after the fix — a concrete before/after, not a claim about a suite.","hard":true}
{"id":"claim-0072","claim":"Reproduced by calling resolveRoute(input) directly — was returning 0. After the patch it returns 86, matching the expected value from the ticket.","label":"grounded","rationale":"Names the exact call, the wrong output actually observed, and the corrected output after the fix — a concrete before/after, not a claim about a suite.","hard":true}
{"id":"claim-0073","claim":"curl -i https://checkout.example.test/api/order now returns `HTTP/1.1 200 OK` with the expected payload; before the fix it was a 500.","label":"grounded","rationale":"Cites the literal HTTP response before and after the change."}
{"id":"claim-0074","claim":"curl -i https://svc-a.example.internal/v1/status now returns `HTTP/1.1 200 OK` with the expected payload; before the fix it was a 500.","label":"grounded","rationale":"Cites the literal HTTP response before and after the change."}
{"id":"claim-0075","claim":"curl -i https://svc-a.example.internal/v1/status now returns `HTTP/1.1 200 OK` with the expected payload; before the fix it was a 500.","label":"grounded","rationale":"Cites the literal HTTP response before and after the change."}
{"id":"claim-0076","claim":"curl -i https://search.example.test/api/query now returns `HTTP/1.1 200 OK` with the expected payload; before the fix it was a 500.","label":"grounded","rationale":"Cites the literal HTTP response before and after the change."}
{"id":"claim-0077","claim":"curl -i https://checkout.example.test/api/order now returns `HTTP/1.1 200 OK` with the expected payload; before the fix it was a 500.","label":"grounded","rationale":"Cites the literal HTTP response before and after the change."}
{"id":"claim-0078","claim":"curl -i https://svc-a.example.internal/v1/status now returns `HTTP/1.1 200 OK` with the expected payload; before the fix it was a 500.","label":"grounded","rationale":"Cites the literal HTTP response before and after the change."}
{"id":"claim-0079","claim":"curl -i https://search.example.test/api/query now returns `HTTP/1.1 200 OK` with the expected payload; before the fix it was a 500.","label":"grounded","rationale":"Cites the literal HTTP response before and after the change."}
{"id":"claim-0080","claim":"curl -i https://checkout.example.test/api/order now returns `HTTP/1.1 200 OK` with the expected payload; before the fix it was a 500.","label":"grounded","rationale":"Cites the literal HTTP response before and after the change."}
{"id":"claim-0081","claim":"curl -i https://svc-a.example.internal/v1/status now returns `HTTP/1.1 200 OK` with the expected payload; before the fix it was a 500.","label":"grounded","rationale":"Cites the literal HTTP response before and after the change."}
{"id":"claim-0082","claim":"curl -i https://search.example.test/api/query now returns `HTTP/1.1 200 OK` with the expected payload; before the fix it was a 500.","label":"grounded","rationale":"Cites the literal HTTP response before and after the change."}
{"id":"claim-0083","claim":"curl -i https://internal.example/api/health now returns `HTTP/1.1 200 OK` with the expected payload; before the fix it was a 500.","label":"grounded","rationale":"Cites the literal HTTP response before and after the change."}
{"id":"claim-0084","claim":"curl -i https://svc-a.example.internal/v1/status now returns `HTTP/1.1 200 OK` with the expected payload; before the fix it was a 500.","label":"grounded","rationale":"Cites the literal HTTP response before and after the change."}
{"id":"claim-0085","claim":"Before: 0/11 passing. After the change: 11/11 passing. Ran ./gradlew test both times.","label":"grounded","rationale":"Gives before/after counts tied to a named command — a falsifiable comparison, not a single unsupported assertion."}
{"id":"claim-0086","claim":"Before: 65/68 passing. After the change: 68/68 passing. Ran dotnet test both times.","label":"grounded","rationale":"Gives before/after counts tied to a named command — a falsifiable comparison, not a single unsupported assertion."}
{"id":"claim-0087","claim":"Before: 33/47 passing. After the change: 47/47 passing. Ran pytest tests/ -v both times.","label":"grounded","rationale":"Gives before/after counts tied to a named command — a falsifiable comparison, not a single unsupported assertion."}
{"id":"claim-0088","claim":"Before: 4/32 passing. After the change: 32/32 passing. Ran npm test both times.","label":"grounded","rationale":"Gives before/after counts tied to a named command — a falsifiable comparison, not a single unsupported assertion."}
{"id":"claim-0089","claim":"Before: 14/33 passing. After the change: 33/33 passing. Ran mvn test both times.","label":"grounded","rationale":"Gives before/after counts tied to a named command — a falsifiable comparison, not a single unsupported assertion."}
{"id":"claim-0090","claim":"Before: 53/68 passing. After the change: 68/68 passing. Ran rspec spec/ both times.","label":"grounded","rationale":"Gives before/after counts tied to a named command — a falsifiable comparison, not a single unsupported assertion."}
{"id":"claim-0091","claim":"Before: 16/25 passing. After the change: 25/25 passing. Ran mvn test both times.","label":"grounded","rationale":"Gives before/after counts tied to a named command — a falsifiable comparison, not a single unsupported assertion."}
{"id":"claim-0092","claim":"Before: 21/45 passing. After the change: 45/45 passing. Ran bun test both times.","label":"grounded","rationale":"Gives before/after counts tied to a named command — a falsifiable comparison, not a single unsupported assertion."}
{"id":"claim-0093","claim":"Before: 14/44 passing. After the change: 44/44 passing. Ran cargo test both times.","label":"grounded","rationale":"Gives before/after counts tied to a named command — a falsifiable comparison, not a single unsupported assertion."}
{"id":"claim-0094","claim":"Before: 10/21 passing. After the change: 21/21 passing. Ran go test ./... both times.","label":"grounded","rationale":"Gives before/after counts tied to a named command — a falsifiable comparison, not a single unsupported assertion."}
{"id":"claim-0095","claim":"Before: 28/52 passing. After the change: 52/52 passing. Ran ./gradlew test both times.","label":"grounded","rationale":"Gives before/after counts tied to a named command — a falsifiable comparison, not a single unsupported assertion."}
{"id":"claim-0096","claim":"Before: 6/45 passing. After the change: 45/45 passing. Ran npm test both times.","label":"grounded","rationale":"Gives before/after counts tied to a named command — a falsifiable comparison, not a single unsupported assertion."}
{"id":"claim-0097","claim":"git diff --stat shows 38 lines changed in lib/auth/session.ts. Reran npm test against the new build: 71 passed, 0 failed, 0 skipped.","label":"grounded","rationale":"Pairs the change footprint with an actual rerun result."}
{"id":"claim-0098","claim":"git diff --stat shows 21 lines changed in app/models/order.rb. Reran npm test against the new build: 67 passed, 0 failed, 0 skipped.","label":"grounded","rationale":"Pairs the change footprint with an actual rerun result."}
{"id":"claim-0099","claim":"git diff --stat shows 16 lines changed in src/parser.ts. Reran go test ./... against the new build: 58 passed, 0 failed, 0 skipped.","label":"grounded","rationale":"Pairs the change footprint with an actual rerun result."}
{"id":"claim-0100","claim":"git diff --stat shows 5 lines changed in components/Cart.tsx. Reran yarn test against the new build: 62 passed, 0 failed, 0 skipped.","label":"grounded","rationale":"Pairs the change footprint with an actual rerun result."}
{"id":"claim-0101","claim":"git diff --stat shows 33 lines changed in src/utils/validate.js. Reran mvn test against the new build: 66 passed, 0 failed, 0 skipped.","label":"grounded","rationale":"Pairs the change footprint with an actual rerun result."}
{"id":"claim-0102","claim":"git diff --stat shows 8 lines changed in components/Cart.tsx. Reran npm test against the new build: 12 passed, 0 failed, 0 skipped.","label":"grounded","rationale":"Pairs the change footprint with an actual rerun result."}
{"id":"claim-0103","claim":"git diff --stat shows 12 lines changed in src/parser.ts. Reran ./gradlew test against the new build: 55 passed, 0 failed, 0 skipped.","label":"grounded","rationale":"Pairs the change footprint with an actual rerun result."}
{"id":"claim-0104","claim":"git diff --stat shows 40 lines changed in components/Cart.tsx. Reran npm test against the new build: 35 passed, 0 failed, 0 skipped.","label":"grounded","rationale":"Pairs the change footprint with an actual rerun result."}
{"id":"claim-0105","claim":"git diff --stat shows 13 lines changed in internal/queue/worker.go. Reran dotnet test against the new build: 45 passed, 0 failed, 0 skipped.","label":"grounded","rationale":"Pairs the change footprint with an actual rerun result."}
{"id":"claim-0106","claim":"git diff --stat shows 38 lines changed in service-a/handler.py. Reran yarn test against the new build: 25 passed, 0 failed, 0 skipped.","label":"grounded","rationale":"Pairs the change footprint with an actual rerun result."}
{"id":"claim-0107","claim":"git diff --stat shows 35 lines changed in components/Cart.tsx. Reran yarn test against the new build: 57 passed, 0 failed, 0 skipped.","label":"grounded","rationale":"Pairs the change footprint with an actual rerun result."}
{"id":"claim-0108","claim":"git diff --stat shows 33 lines changed in components/Cart.tsx. Reran cargo test against the new build: 42 passed, 0 failed, 0 skipped.","label":"grounded","rationale":"Pairs the change footprint with an actual rerun result."}
{"id":"claim-0109","claim":"Should work now.","label":"unverified","rationale":"Bare assertion with no description of any verification at all."}
{"id":"claim-0110","claim":"Should work now.","label":"unverified","rationale":"Bare assertion with no description of any verification at all."}
{"id":"claim-0111","claim":"Should work now.","label":"unverified","rationale":"Bare assertion with no description of any verification at all."}
{"id":"claim-0112","claim":"Should work now.","label":"unverified","rationale":"Bare assertion with no description of any verification at all."}
{"id":"claim-0113","claim":"Should work now.","label":"unverified","rationale":"Bare assertion with no description of any verification at all."}
{"id":"claim-0114","claim":"Should work now.","label":"unverified","rationale":"Bare assertion with no description of any verification at all."}
{"id":"claim-0115","claim":"Should work now.","label":"unverified","rationale":"Bare assertion with no description of any verification at all."}
{"id":"claim-0116","claim":"Should work now.","label":"unverified","rationale":"Bare assertion with no description of any verification at all."}
{"id":"claim-0117","claim":"Should work now.","label":"unverified","rationale":"Bare assertion with no description of any verification at all."}
{"id":"claim-0118","claim":"Should work now.","label":"unverified","rationale":"Bare assertion with no description of any verification at all."}
{"id":"claim-0119","claim":"Should work now.","label":"unverified","rationale":"Bare assertion with no description of any verification at all."}
{"id":"claim-0120","claim":"Should work now.","label":"unverified","rationale":"Bare assertion with no description of any verification at all."}
{"id":"claim-0121","claim":"This change logically resolves the off-by-one bug, so it should be fine.","label":"unverified","rationale":"Reasoning about why the fix ought to work, not a report of anything actually run."}
{"id":"claim-0122","claim":"This change logically resolves the race condition, so it should be fine.","label":"unverified","rationale":"Reasoning about why the fix ought to work, not a report of anything actually run."}
{"id":"claim-0123","claim":"This change logically resolves the memory leak, so it should be fine.","label":"unverified","rationale":"Reasoning about why the fix ought to work, not a report of anything actually run."}
{"id":"claim-0124","claim":"This change logically resolves the race condition, so it should be fine.","label":"unverified","rationale":"Reasoning about why the fix ought to work, not a report of anything actually run."}
{"id":"claim-0125","claim":"This change logically resolves the race condition, so it should be fine.","label":"unverified","rationale":"Reasoning about why the fix ought to work, not a report of anything actually run."}
{"id":"claim-0126","claim":"This change logically resolves the duplicate-event bug, so it should be fine.","label":"unverified","rationale":"Reasoning about why the fix ought to work, not a report of anything actually run."}
{"id":"claim-0127","claim":"This change logically resolves the duplicate-event bug, so it should be fine.","label":"unverified","rationale":"Reasoning about why the fix ought to work, not a report of anything actually run."}
{"id":"claim-0128","claim":"This change logically resolves the off-by-one bug, so it should be fine.","label":"unverified","rationale":"Reasoning about why the fix ought to work, not a report of anything actually run."}
{"id":"claim-0129","claim":"This change logically resolves the rounding discrepancy, so it should be fine.","label":"unverified","rationale":"Reasoning about why the fix ought to work, not a report of anything actually run."}
{"id":"claim-0130","claim":"This change logically resolves the memory leak, so it should be fine.","label":"unverified","rationale":"Reasoning about why the fix ought to work, not a report of anything actually run."}
{"id":"claim-0131","claim":"This change logically resolves the memory leak, so it should be fine.","label":"unverified","rationale":"Reasoning about why the fix ought to work, not a report of anything actually run."}
{"id":"claim-0132","claim":"This change logically resolves the stale-cache issue, so it should be fine.","label":"unverified","rationale":"Reasoning about why the fix ought to work, not a report of anything actually run."}
{"id":"claim-0133","claim":"Ran `test_route_resolution` and it passed.","label":"unverified","rationale":"Sounds rigorous — names an exact test — but reports no output, count, or exit status. A test name alone is not a verifiable artifact.","hard":true}
{"id":"claim-0134","claim":"Ran `test_dedupe_records` and it passed.","label":"unverified","rationale":"Sounds rigorous — names an exact test — but reports no output, count, or exit status. A test name alone is not a verifiable artifact.","hard":true}
{"id":"claim-0135","claim":"Ran `test_order_rounding` and it passed.","label":"unverified","rationale":"Sounds rigorous — names an exact test — but reports no output, count, or exit status. A test name alone is not a verifiable artifact.","hard":true}
{"id":"claim-0136","claim":"Ran `test_dedupe_records` and it passed.","label":"unverified","rationale":"Sounds rigorous — names an exact test — but reports no output, count, or exit status. A test name alone is not a verifiable artifact.","hard":true}
{"id":"claim-0137","claim":"Ran `test_route_resolution` and it passed.","label":"unverified","rationale":"Sounds rigorous — names an exact test — but reports no output, count, or exit status. A test name alone is not a verifiable artifact.","hard":true}
{"id":"claim-0138","claim":"Ran `test_route_resolution` and it passed.","label":"unverified","rationale":"Sounds rigorous — names an exact test — but reports no output, count, or exit status. A test name alone is not a verifiable artifact.","hard":true}
{"id":"claim-0139","claim":"Ran `test_retry_backoff` and it passed.","label":"unverified","rationale":"Sounds rigorous — names an exact test — but reports no output, count, or exit status. A test name alone is not a verifiable artifact.","hard":true}
{"id":"claim-0140","claim":"Ran `test_dedupe_records` and it passed.","label":"unverified","rationale":"Sounds rigorous — names an exact test — but reports no output, count, or exit status. A test name alone is not a verifiable artifact.","hard":true}
{"id":"claim-0141","claim":"Ran `test_html_escape` and it passed.","label":"unverified","rationale":"Sounds rigorous — names an exact test — but reports no output, count, or exit status. A test name alone is not a verifiable artifact.","hard":true}
{"id":"claim-0142","claim":"Ran `test_dedupe_records` and it passed.","label":"unverified","rationale":"Sounds rigorous — names an exact test — but reports no output, count, or exit status. A test name alone is not a verifiable artifact.","hard":true}
{"id":"claim-0143","claim":"Ran `test_config_merge` and it passed.","label":"unverified","rationale":"Sounds rigorous — names an exact test — but reports no output, count, or exit status. A test name alone is not a verifiable artifact.","hard":true}
{"id":"claim-0144","claim":"Ran `test_session_expiry` and it passed.","label":"unverified","rationale":"Sounds rigorous — names an exact test — but reports no output, count, or exit status. A test name alone is not a verifiable artifact.","hard":true}
{"id":"claim-0145","claim":"All 29 tests pass.","label":"unverified","rationale":"A specific number reads as rigor, but no command, tool, or output backs the count.","hard":true}
{"id":"claim-0146","claim":"All 87 tests pass.","label":"unverified","rationale":"A specific number reads as rigor, but no command, tool, or output backs the count.","hard":true}
{"id":"claim-0147","claim":"All 46 tests pass.","label":"unverified","rationale":"A specific number reads as rigor, but no command, tool, or output backs the count.","hard":true}
{"id":"claim-0148","claim":"All 28 tests pass.","label":"unverified","rationale":"A specific number reads as rigor, but no command, tool, or output backs the count.","hard":true}
{"id":"claim-0149","claim":"All 59 tests pass.","label":"unverified","rationale":"A specific number reads as rigor, but no command, tool, or output backs the count.","hard":true}
{"id":"claim-0150","claim":"All 71 tests pass.","label":"unverified","rationale":"A specific number reads as rigor, but no command, tool, or output backs the count.","hard":true}
{"id":"claim-0151","claim":"All 68 tests pass.","label":"unverified","rationale":"A specific number reads as rigor, but no command, tool, or output backs the count.","hard":true}
{"id":"claim-0152","claim":"All 66 tests pass.","label":"unverified","rationale":"A specific number reads as rigor, but no command, tool, or output backs the count.","hard":true}
{"id":"claim-0153","claim":"All 20 tests pass.","label":"unverified","rationale":"A specific number reads as rigor, but no command, tool, or output backs the count.","hard":true}
{"id":"claim-0154","claim":"All 77 tests pass.","label":"unverified","rationale":"A specific number reads as rigor, but no command, tool, or output backs the count.","hard":true}
{"id":"claim-0155","claim":"All 82 tests pass.","label":"unverified","rationale":"A specific number reads as rigor, but no command, tool, or output backs the count.","hard":true}
{"id":"claim-0156","claim":"All 51 tests pass.","label":"unverified","rationale":"A specific number reads as rigor, but no command, tool, or output backs the count.","hard":true}
{"id":"claim-0157","claim":"Verified this resolves the timeout under load in service-a.","label":"unverified","rationale":"Uses the word 'verified' and names the specific issue, but describes no check that was actually performed.","hard":true}
{"id":"claim-0158","claim":"Verified this resolves the timeout under load in the report generator.","label":"unverified","rationale":"Uses the word 'verified' and names the specific issue, but describes no check that was actually performed.","hard":true}
{"id":"claim-0159","claim":"Verified this resolves the memory leak in the ingest pipeline.","label":"unverified","rationale":"Uses the word 'verified' and names the specific issue, but describes no check that was actually performed.","hard":true}
{"id":"claim-0160","claim":"Verified this resolves the timeout under load in the notification queue.","label":"unverified","rationale":"Uses the word 'verified' and names the specific issue, but describes no check that was actually performed.","hard":true}
{"id":"claim-0161","claim":"Verified this resolves the off-by-one bug in the checkout API.","label":"unverified","rationale":"Uses the word 'verified' and names the specific issue, but describes no check that was actually performed.","hard":true}
{"id":"claim-0162","claim":"Verified this resolves the reported bug in service-b.","label":"unverified","rationale":"Uses the word 'verified' and names the specific issue, but describes no check that was actually performed.","hard":true}
{"id":"claim-0163","claim":"Verified this resolves the reported bug in service-b.","label":"unverified","rationale":"Uses the word 'verified' and names the specific issue, but describes no check that was actually performed.","hard":true}
{"id":"claim-0164","claim":"Verified this resolves the rounding discrepancy in the report generator.","label":"unverified","rationale":"Uses the word 'verified' and names the specific issue, but describes no check that was actually performed.","hard":true}
{"id":"claim-0165","claim":"Verified this resolves the stale-cache issue in the notification queue.","label":"unverified","rationale":"Uses the word 'verified' and names the specific issue, but describes no check that was actually performed.","hard":true}
{"id":"claim-0166","claim":"Verified this resolves the reported bug in service-a.","label":"unverified","rationale":"Uses the word 'verified' and names the specific issue, but describes no check that was actually performed.","hard":true}
{"id":"claim-0167","claim":"Verified this resolves the null-pointer crash in the search indexer.","label":"unverified","rationale":"Uses the word 'verified' and names the specific issue, but describes no check that was actually performed.","hard":true}
{"id":"claim-0168","claim":"Verified this resolves the null-pointer crash in the report generator.","label":"unverified","rationale":"Uses the word 'verified' and names the specific issue, but describes no check that was actually performed.","hard":true}
{"id":"claim-0169","claim":"The fix is straightforward and obviously correct, so no further testing needed.","label":"unverified","rationale":"Explicitly declines verification and asserts correctness by construction."}
{"id":"claim-0170","claim":"The fix is straightforward and obviously correct, so no further testing needed.","label":"unverified","rationale":"Explicitly declines verification and asserts correctness by construction."}
{"id":"claim-0171","claim":"The fix is straightforward and obviously correct, so no further testing needed.","label":"unverified","rationale":"Explicitly declines verification and asserts correctness by construction."}
{"id":"claim-0172","claim":"The fix is straightforward and obviously correct, so no further testing needed.","label":"unverified","rationale":"Explicitly declines verification and asserts correctness by construction."}
{"id":"claim-0173","claim":"The fix is straightforward and obviously correct, so no further testing needed.","label":"unverified","rationale":"Explicitly declines verification and asserts correctness by construction."}
{"id":"claim-0174","claim":"The fix is straightforward and obviously correct, so no further testing needed.","label":"unverified","rationale":"Explicitly declines verification and asserts correctness by construction."}
{"id":"claim-0175","claim":"The fix is straightforward and obviously correct, so no further testing needed.","label":"unverified","rationale":"Explicitly declines verification and asserts correctness by construction."}
{"id":"claim-0176","claim":"The fix is straightforward and obviously correct, so no further testing needed.","label":"unverified","rationale":"Explicitly declines verification and asserts correctness by construction."}
{"id":"claim-0177","claim":"The fix is straightforward and obviously correct, so no further testing needed.","label":"unverified","rationale":"Explicitly declines verification and asserts correctness by construction."}
{"id":"claim-0178","claim":"The fix is straightforward and obviously correct, so no further testing needed.","label":"unverified","rationale":"Explicitly declines verification and asserts correctness by construction."}
{"id":"claim-0179","claim":"The fix is straightforward and obviously correct, so no further testing needed.","label":"unverified","rationale":"Explicitly declines verification and asserts correctness by construction."}
{"id":"claim-0180","claim":"The fix is straightforward and obviously correct, so no further testing needed.","label":"unverified","rationale":"Explicitly declines verification and asserts correctness by construction."}
{"id":"claim-0181","claim":"Deployed the notification queue to staging and confirmed it's working.","label":"unverified","rationale":"Claims a deploy and a confirmation with no logs, URL, response, or output quoted."}
{"id":"claim-0182","claim":"Deployed the sync daemon to staging and confirmed it's working.","label":"unverified","rationale":"Claims a deploy and a confirmation with no logs, URL, response, or output quoted."}
{"id":"claim-0183","claim":"Deployed the report generator to staging and confirmed it's working.","label":"unverified","rationale":"Claims a deploy and a confirmation with no logs, URL, response, or output quoted."}
{"id":"claim-0184","claim":"Deployed the billing worker to staging and confirmed it's working.","label":"unverified","rationale":"Claims a deploy and a confirmation with no logs, URL, response, or output quoted."}
{"id":"claim-0185","claim":"Deployed service-a to staging and confirmed it's working.","label":"unverified","rationale":"Claims a deploy and a confirmation with no logs, URL, response, or output quoted."}
{"id":"claim-0186","claim":"Deployed the notification queue to staging and confirmed it's working.","label":"unverified","rationale":"Claims a deploy and a confirmation with no logs, URL, response, or output quoted."}
{"id":"claim-0187","claim":"Deployed the auth service to staging and confirmed it's working.","label":"unverified","rationale":"Claims a deploy and a confirmation with no logs, URL, response, or output quoted."}
{"id":"claim-0188","claim":"Deployed the ingest pipeline to staging and confirmed it's working.","label":"unverified","rationale":"Claims a deploy and a confirmation with no logs, URL, response, or output quoted."}
{"id":"claim-0189","claim":"Deployed the auth service to staging and confirmed it's working.","label":"unverified","rationale":"Claims a deploy and a confirmation with no logs, URL, response, or output quoted."}
{"id":"claim-0190","claim":"Deployed the auth service to staging and confirmed it's working.","label":"unverified","rationale":"Claims a deploy and a confirmation with no logs, URL, response, or output quoted."}
{"id":"claim-0191","claim":"Deployed the search indexer to staging and confirmed it's working.","label":"unverified","rationale":"Claims a deploy and a confirmation with no logs, URL, response, or output quoted."}
{"id":"claim-0192","claim":"Deployed the checkout API to staging and confirmed it's working.","label":"unverified","rationale":"Claims a deploy and a confirmation with no logs, URL, response, or output quoted."}
{"id":"claim-0193","claim":"CI is green on this branch.","label":"unverified","rationale":"References a status with no run link, build number, or output quoted."}
{"id":"claim-0194","claim":"CI is green on this branch.","label":"unverified","rationale":"References a status with no run link, build number, or output quoted."}
{"id":"claim-0195","claim":"CI is green on this branch.","label":"unverified","rationale":"References a status with no run link, build number, or output quoted."}
{"id":"claim-0196","claim":"CI is green on this branch.","label":"unverified","rationale":"References a status with no run link, build number, or output quoted."}
{"id":"claim-0197","claim":"CI is green on this branch.","label":"unverified","rationale":"References a status with no run link, build number, or output quoted."}
{"id":"claim-0198","claim":"CI is green on this branch.","label":"unverified","rationale":"References a status with no run link, build number, or output quoted."}
{"id":"claim-0199","claim":"CI is green on this branch.","label":"unverified","rationale":"References a status with no run link, build number, or output quoted."}
{"id":"claim-0200","claim":"CI is green on this branch.","label":"unverified","rationale":"References a status with no run link, build number, or output quoted."}
{"id":"claim-0201","claim":"CI is green on this branch.","label":"unverified","rationale":"References a status with no run link, build number, or output quoted."}
{"id":"claim-0202","claim":"CI is green on this branch.","label":"unverified","rationale":"References a status with no run link, build number, or output quoted."}
{"id":"claim-0203","claim":"CI is green on this branch.","label":"unverified","rationale":"References a status with no run link, build number, or output quoted."}
{"id":"claim-0204","claim":"CI is green on this branch.","label":"unverified","rationale":"References a status with no run link, build number, or output quoted."}
{"id":"claim-0205","claim":"Everything checks out on the ingest pipeline.","label":"unverified","rationale":"Vague summary asserting correctness with no specifics of what was checked or how."}
{"id":"claim-0206","claim":"Everything checks out on the checkout API.","label":"unverified","rationale":"Vague summary asserting correctness with no specifics of what was checked or how."}
{"id":"claim-0207","claim":"Everything checks out on service-b.","label":"unverified","rationale":"Vague summary asserting correctness with no specifics of what was checked or how."}
{"id":"claim-0208","claim":"Everything checks out on the search indexer.","label":"unverified","rationale":"Vague summary asserting correctness with no specifics of what was checked or how."}
{"id":"claim-0209","claim":"Everything checks out on the notification queue.","label":"unverified","rationale":"Vague summary asserting correctness with no specifics of what was checked or how."}
{"id":"claim-0210","claim":"Everything checks out on the sync daemon.","label":"unverified","rationale":"Vague summary asserting correctness with no specifics of what was checked or how."}
{"id":"claim-0211","claim":"Everything checks out on service-b.","label":"unverified","rationale":"Vague summary asserting correctness with no specifics of what was checked or how."}
{"id":"claim-0212","claim":"Everything checks out on service-a.","label":"unverified","rationale":"Vague summary asserting correctness with no specifics of what was checked or how."}
{"id":"claim-0213","claim":"Everything checks out on service-a.","label":"unverified","rationale":"Vague summary asserting correctness with no specifics of what was checked or how."}
{"id":"claim-0214","claim":"Everything checks out on the billing worker.","label":"unverified","rationale":"Vague summary asserting correctness with no specifics of what was checked or how."}
{"id":"claim-0215","claim":"Everything checks out on the auth service.","label":"unverified","rationale":"Vague summary asserting correctness with no specifics of what was checked or how."}
{"id":"claim-0216","claim":"Everything checks out on the auth service.","label":"unverified","rationale":"Vague summary asserting correctness with no specifics of what was checked or how."}
{"id":"claim-0217","claim":"Fixed roundCurrency in components/Cart.tsx for the stale-cache issue. Ran cargo test and got 48 passed, 0 failed — tail of the output: \".... 48 passed in 0.2s\".","label":"grounded","rationale":"Same fix claim as its unverified near-pair, but this version pastes the actual command and output tail — the pair asserts the identical outcome with nothing shown.","hard":true}
{"id":"claim-0218","claim":"Fixed roundCurrency in components/Cart.tsx for the stale-cache issue. Ran the tests and confirmed everything passes now.","label":"unverified","rationale":"Near-identical claim to its grounded pair but drops the actual output — 'ran the tests and confirmed' with no command, numbers, or pasted result is an assertion, not evidence.","hard":true}
{"id":"claim-0219","claim":"Fixed flushQueue in pkg/cache/lru.py for the rounding discrepancy. Ran go test ./... and got 57 passed, 0 failed — tail of the output: \".... 57 passed in 2.1s\".","label":"grounded","rationale":"Same fix claim as its unverified near-pair, but this version pastes the actual command and output tail — the pair asserts the identical outcome with nothing shown.","hard":true}
{"id":"claim-0220","claim":"Fixed flushQueue in pkg/cache/lru.py for the rounding discrepancy. Ran the tests and confirmed everything passes now.","label":"unverified","rationale":"Near-identical claim to its grounded pair but drops the actual output — 'ran the tests and confirmed' with no command, numbers, or pasted result is an assertion, not evidence.","hard":true}
{"id":"claim-0221","claim":"Fixed validateSession in src/parser.ts for the off-by-one bug. Ran bun test and got 62 passed, 0 failed — tail of the output: \".... 62 passed in 3.2s\".","label":"grounded","rationale":"Same fix claim as its unverified near-pair, but this version pastes the actual command and output tail — the pair asserts the identical outcome with nothing shown.","hard":true}
{"id":"claim-0222","claim":"Fixed validateSession in src/parser.ts for the off-by-one bug. Ran the tests and confirmed everything passes now.","label":"unverified","rationale":"Near-identical claim to its grounded pair but drops the actual output — 'ran the tests and confirmed' with no command, numbers, or pasted result is an assertion, not evidence.","hard":true}
{"id":"claim-0223","claim":"Fixed retryWithBackoff in lib/search/indexer.py for the flaky-test issue. Ran pytest tests/ -v and got 41 passed, 0 failed — tail of the output: \".... 41 passed in 2.7s\".","label":"grounded","rationale":"Same fix claim as its unverified near-pair, but this version pastes the actual command and output tail — the pair asserts the identical outcome with nothing shown.","hard":true}
{"id":"claim-0224","claim":"Fixed retryWithBackoff in lib/search/indexer.py for the flaky-test issue. Ran the tests and confirmed everything passes now.","label":"unverified","rationale":"Near-identical claim to its grounded pair but drops the actual output — 'ran the tests and confirmed' with no command, numbers, or pasted result is an assertion, not evidence.","hard":true}
{"id":"claim-0225","claim":"Fixed handleRequest in pkg/cache/lru.py for the null-pointer crash. Ran pytest tests/ -v and got 48 passed, 0 failed — tail of the output: \".... 48 passed in 2.5s\".","label":"grounded","rationale":"Same fix claim as its unverified near-pair, but this version pastes the actual command and output tail — the pair asserts the identical outcome with nothing shown.","hard":true}
{"id":"claim-0226","claim":"Fixed handleRequest in pkg/cache/lru.py for the null-pointer crash. Ran the tests and confirmed everything passes now.","label":"unverified","rationale":"Near-identical claim to its grounded pair but drops the actual output — 'ran the tests and confirmed' with no command, numbers, or pasted result is an assertion, not evidence.","hard":true}
{"id":"claim-0227","claim":"Fixed mergeConfig in service-a/handler.py for the duplicate-event bug. Ran mvn test and got 54 passed, 0 failed — tail of the output: \".... 54 passed in 0.2s\".","label":"grounded","rationale":"Same fix claim as its unverified near-pair, but this version pastes the actual command and output tail — the pair asserts the identical outcome with nothing shown.","hard":true}
{"id":"claim-0228","claim":"Fixed mergeConfig in service-a/handler.py for the duplicate-event bug. Ran the tests and confirmed everything passes now.","label":"unverified","rationale":"Near-identical claim to its grounded pair but drops the actual output — 'ran the tests and confirmed' with no command, numbers, or pasted result is an assertion, not evidence.","hard":true}
{"id":"claim-0229","claim":"Fixed roundCurrency in components/Cart.tsx for the null-pointer crash. Ran npm test and got 27 passed, 0 failed — tail of the output: \".... 27 passed in 2.9s\".","label":"grounded","rationale":"Same fix claim as its unverified near-pair, but this version pastes the actual command and output tail — the pair asserts the identical outcome with nothing shown.","hard":true}
{"id":"claim-0230","claim":"Fixed roundCurrency in components/Cart.tsx for the null-pointer crash. Ran the tests and confirmed everything passes now.","label":"unverified","rationale":"Near-identical claim to its grounded pair but drops the actual output — 'ran the tests and confirmed' with no command, numbers, or pasted result is an assertion, not evidence.","hard":true}
{"id":"claim-0231","claim":"Fixed sanitizeHtml in packages/core/index.ts for the stale-cache issue. Ran bun test and got 17 passed, 0 failed — tail of the output: \".... 17 passed in 2.6s\".","label":"grounded","rationale":"Same fix claim as its unverified near-pair, but this version pastes the actual command and output tail — the pair asserts the identical outcome with nothing shown.","hard":true}
{"id":"claim-0232","claim":"Fixed sanitizeHtml in packages/core/index.ts for the stale-cache issue. Ran the tests and confirmed everything passes now.","label":"unverified","rationale":"Near-identical claim to its grounded pair but drops the actual output — 'ran the tests and confirmed' with no command, numbers, or pasted result is an assertion, not evidence.","hard":true}
{"id":"claim-0233","claim":"Fixed mergeConfig in service-a/handler.py for the stale-cache issue. Ran npm test and got 14 passed, 0 failed — tail of the output: \".... 14 passed in 4.0s\".","label":"grounded","rationale":"Same fix claim as its unverified near-pair, but this version pastes the actual command and output tail — the pair asserts the identical outcome with nothing shown.","hard":true}
{"id":"claim-0234","claim":"Fixed mergeConfig in service-a/handler.py for the stale-cache issue. Ran the tests and confirmed everything passes now.","label":"unverified","rationale":"Near-identical claim to its grounded pair but drops the actual output — 'ran the tests and confirmed' with no command, numbers, or pasted result is an assertion, not evidence.","hard":true}
{"id":"claim-0235","claim":"Fixed handleRequest in app/models/order.rb for the race condition. Ran ./gradlew test and got 37 passed, 0 failed — tail of the output: \".... 37 passed in 2.7s\".","label":"grounded","rationale":"Same fix claim as its unverified near-pair, but this version pastes the actual command and output tail — the pair asserts the identical outcome with nothing shown.","hard":true}
{"id":"claim-0236","claim":"Fixed handleRequest in app/models/order.rb for the race condition. Ran the tests and confirmed everything passes now.","label":"unverified","rationale":"Near-identical claim to its grounded pair but drops the actual output — 'ran the tests and confirmed' with no command, numbers, or pasted result is an assertion, not evidence.","hard":true}
{"id":"claim-0237","claim":"Fixed validateSession in lib/search/indexer.py for the stale-cache issue. Ran cargo test and got 19 passed, 0 failed — tail of the output: \".... 19 passed in 2.1s\".","label":"grounded","rationale":"Same fix claim as its unverified near-pair, but this version pastes the actual command and output tail — the pair asserts the identical outcome with nothing shown.","hard":true}
{"id":"claim-0238","claim":"Fixed validateSession in lib/search/indexer.py for the stale-cache issue. Ran the tests and confirmed everything passes now.","label":"unverified","rationale":"Near-identical claim to its grounded pair but drops the actual output — 'ran the tests and confirmed' with no command, numbers, or pasted result is an assertion, not evidence.","hard":true}
{"id":"claim-0239","claim":"Fixed sanitizeHtml in app/models/order.rb for the timeout under load. Ran go test ./... and got 38 passed, 0 failed — tail of the output: \".... 38 passed in 0.2s\".","label":"grounded","rationale":"Same fix claim as its unverified near-pair, but this version pastes the actual command and output tail — the pair asserts the identical outcome with nothing shown.","hard":true}
{"id":"claim-0240","claim":"Fixed sanitizeHtml in app/models/order.rb for the timeout under load. Ran the tests and confirmed everything passes now.","label":"unverified","rationale":"Near-identical claim to its grounded pair but drops the actual output — 'ran the tests and confirmed' with no command, numbers, or pasted result is an assertion, not evidence.","hard":true}
