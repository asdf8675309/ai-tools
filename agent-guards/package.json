{
  "name": "agent-guards",
  "version": "1.0.0",
  "description": "Claude Code hooks that stop an agent from reporting success it never verified, running a check that tests the wrong thing, or leaking into a public repo",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
