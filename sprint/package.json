{
  "name": "sprint",
  "version": "1.0.0",
  "description": "Dispatch N parallel coding agents against a GitHub issue queue, one worktree each, each reviewed before its PR opens",
  "license": "MIT",
  "type": "module",
  "private": true,
  "engines": {
    "bun": ">=1.3.0"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
